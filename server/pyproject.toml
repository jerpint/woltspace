[project]
name = "woltspace-server"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "websockets",
    "httpx",
    "watchfiles",
    "python-dotenv",
    "jinja2",
]
