{
  "name": "woltspace",
  "type": "module",
  "private": true,
  "scripts": {
    "build:chat": "esbuild web/chat.js --bundle --format=esm --platform=browser --outfile=public/static/chat.js && mkdir -p public/static/pkg && cp node_modules/@matrix-org/matrix-sdk-crypto-wasm/pkg/matrix_sdk_crypto_wasm_bg.wasm public/static/pkg/",
    "test": "node --test test/**/*.test.mjs tui/test/**/*.test.mjs"
  },
  "dependencies": {
    "matrix-js-sdk": "42.4.0",
    "node-pty": "^1.1.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
