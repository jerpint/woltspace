[project]
name = "woltspace"
version = "0.5.8"
description = "A persistent lodge for local AI agent sessions"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "websockets",
    "httpx",
    "watchfiles",
    "python-dotenv",
    "jinja2",
    # The CLI's voice. Pure Python, no compiled extensions, one transitive
    # dependency tree already common in this stack — and it is the piece that
    # makes the lodge palette safe: it drops every escape when stdout is not a
    # terminal, so `woltspace status | cat` stays plain parseable text.
    "rich",
    "litellm",
    "openai",
    "python-telegram-bot",
    "slack-bolt",
    "slack-sdk",
    "pyjwt[crypto]>=2.12.1",
]

[project.scripts]
woltspace = "woltspace.cli:main"

[project.optional-dependencies]
test = [
    "pytest",
    "pytest-asyncio",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
exclude = [
    "**/__pycache__/**",
    "**/*.pyc",
    "container/bot/test_*.py",
]

[tool.hatch.build.targets.wheel]
packages = ["src/woltspace"]

[tool.hatch.build.targets.wheel.force-include]
"server" = "woltspace/_bundle/server"
"container" = "woltspace/_bundle/container"
"public" = "woltspace/_bundle/public"
"template" = "woltspace/_bundle/template"
"templates" = "woltspace/_bundle/templates"
"docs/updates.md" = "woltspace/_bundle/docs/updates.md"
"docs/shared-skills.md" = "woltspace/_bundle/docs/shared-skills.md"
"docs/colony-seeds.md" = "woltspace/_bundle/docs/colony-seeds.md"
"docs/digging-v0.md" = "woltspace/_bundle/docs/digging-v0.md"
"docs/slack-dm-agent-mvp.md" = "woltspace/_bundle/docs/slack-dm-agent-mvp.md"
"docs/sandboxed-adapter-callbacks.md" = "woltspace/_bundle/docs/sandboxed-adapter-callbacks.md"

[tool.hatch.build.targets.sdist]
include = [
    "/src/woltspace",
    "/server",
    "/container",
    "/public",
    "/template",
    "/templates",
    "/README.md",
    "/docs/updates.md",
    "/docs/shared-skills.md",
    "/docs/colony-seeds.md",
    "/docs/digging-v0.md",
    "/docs/slack-dm-agent-mvp.md",
    "/docs/sandboxed-adapter-callbacks.md",
    "/LICENSE",
]
